# Requestly Environment Configuration
# Copy this file to .env.local for local development.
# Real credentials must NEVER be committed to version control.

# MongoDB Atlas Connection (Implemented in future phase)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/requestly?retryWrites=true&w=majority
# MONGODB_DATABASE=requestly
